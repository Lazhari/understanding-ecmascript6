'use strict';

function getValue(condition) {
    
    if(condition) {
        let value = "blue";

        // other code

        return value;
    } else {
        //Value doesn't exist here
        
        return null
    }

    // Value does't exist here 

}
